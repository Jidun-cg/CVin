VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# JWT secret used by custom auth endpoints (DO NOT expose real secret in client)
JWT_SECRET=replace_with_strong_secret
# Service role key (only for serverless API usage, NEVER commit real value)
SUPABASE_SERVICE_KEY=
# Public bucket name for payment proofs
SUPABASE_BUCKET=payment-proofs
